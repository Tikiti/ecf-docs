# CRITICAL FINDING: Footnote Marker Format Issue

## Discovery

Checked field 34, type 44 in the actual CSV file:
```
Column 13 (type 44): '271'
```

## What This Means

The CSV stores **obligatoriedad WITH footnote markers as merged strings**:
- `271` means: obligatoriedad = `2`, footnote marker = `⁷¹`
- `214` means: obligatoriedad = `2`, footnote marker = `¹⁴`  
- `216` means: obligatoriedad = `2`, footnote marker = `¹⁶`

In the markdown, I correctly separated these as `2⁷¹`, `2¹⁴`, `2¹⁶` etc.

## Impact on Verification

**The "footnote marker" errors (8 fields) are FALSE POSITIVES!**

They're not real errors - just artifacts of how the CSV stores combined data vs how markdown displays it.

## Revised Error Count

- **Original report: 75 errors**
- **Footnote false positives: 8 errors**
- **ACTUAL ERRORS: 67 errors** (all in Sections C and E)

## Real Errors Summary

### Section C (SUBTOTALES) - Fields 7-13
- 63 real errors
- Pattern: Used `2` instead of `3` for optional section

### Section E (PAGINACIÓN) - Fields 3-5  
- 4-6 real errors
- Pattern: Mixed up mandatory/conditional/optional codes

### Total Real Errors: ~67

## Recommendation

Update verification script to:
1. Parse footnote markers from CSV (e.g., `271` → `2` with footnote `71`)
2. Parse footnote markers from MD (e.g., `2⁷¹` → `2` with footnote `71`)
3. Compare only the base obligatoriedad values

This will eliminate false positives and show only real errors.
